'use strict';

/**
 * WIP
 * A high-level 3D object composed of groups of vertices and faces.
 */
export class Mesh {
  constructor(opts) {
    _.noop(opts);
    // TODO: Not sure what would be the best way to represent this yet.
  }
}
