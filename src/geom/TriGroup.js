'use strict';

/**
 * WIP
 * A group of tris rendered using the same material.
 */
export class TriGroup {
  /**
   * @param {Vert[]} verts
   * @param {Tri[]} tris
   * @param {Material} material
   * @param {TriGroupOpts} [opts]
   */
  constructor(verts, tris, material, opts) {
    _.noop(verts, tris, material, opts); // TODO
  }
}
